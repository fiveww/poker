# 朋友局德州扑克 · 技术方案

> 面向 2–10 人朋友局德州扑克(Texas Hold'em),**仅微信小程序端**,后端用微信云开发(CloudBase),体验版运行,免审核免备案。卡图打包在前端。

---

## 1. 目标与约束

| 项 | 说明 |
|----|------|
| 玩家人数 | 2–10 |
| 玩法 | 德州扑克(无限注或固定注,开房者配置) |
| 分发形态 | **仅微信小程序**,网页端暂不做 |
| 运行形态 | 体验版,仅体验成员可进,不走审核 |
| 卡面资源 | 打包进前端,不走云存储/CDN |
| 账号 | 小程序 openid,稳定身份 |
| 出牌时限 | **无时间限制**,玩家可慢慢想;离线由房主/投票处理 |
| 重连 | 每手状态持久化,断线重连续接,不丢进度 |
| 牌局记录 | 记录最近 N 局(默认 10)+ 收藏(上限 10) |
| 盲注 | 开房者设定,**固定不涨盲**(不随手数递增) |
| 初始筹码 | 开房者设定 |
| 借款 | **默认关闭**;开启需开房者手动设参数;条件含"筹码归零方可借";可设还款倍率;**游戏结束默认强制还款,余额可为负** |
| 投票结束 | 每个下注轮结束后可发起投票,**全员同意**才通过;通过则**当前手无效,回退到上一手开完牌、判完输赢、庄家按钮移动前**的状态后结束整局 |
| 开牌(亮牌) | 1)当前手只剩一人未弃牌(前面的人都弃了、后面没人)→ 该人直接获胜,**默认不亮牌**,可选"开牌";2)**任何时候,已弃牌的人默认不亮牌**,可选"开牌";多人到摊牌阶段,服务器只公布赢家的牌(领奖必需),其余人默认不亮、可选开 |
| 成本预期 | 按量计费,月费个位数元以内 |

---

## 2. 整体架构

```
┌──────────────┐
│ 微信小程序端 │   ← 仅此一端
└──────┬───────┘
       │  callFunction + DB realtime listener (watch)
       ▼
   ┌────────────────────────────────┐
   │      微信云开发 (CloudBase)     │
   │  ┌──────────┐  ┌────────────┐ │
   │  │ 云数据库  │  │  云函数    │ │
   │  │ (实时推送)│  │ (权威发牌) │ │
   │  └──────────┘  └────────────┘ │
   └────────────────────────────────┘
```

**核心原则:云函数 = 权威荷官(dealer),云数据库 = 状态广播 + 持久化通道。**
所有牌的生成、洗牌、发牌、动作校验、牌型判定、边池、借款/还款、投票结算都在云函数完成,客户端只负责"展示 + 发动作"。每手状态全程存库,断线重连 watch 续上。

---

## 3. 技术选型

| 层 | 选型 | 理由 |
|----|------|------|
| 小程序框架 | 原生小程序 | 朋友局够用,直连云开发,openid 自动 |
| 卡图 | 纯 CSS 绘制(`components/card`) | 零图片依赖、免素材;雪碧图留作可选美术增强 |
| 后端 | 微信云开发 CloudBase | 免服务器/域名/备案,实时推送 |
| 数据库 | CloudBase 云数据库(实时) | 出牌广播 + 持久化重连 |
| 云函数 | Node.js | 德扑逻辑、牌型判定、借款/投票 |
| 牌型判定库 | **内置评牌器**(action 云函数内,C(7,5)=21 组合穷举打分) | 零依赖免安装,逻辑透明可测(P4 实际采用,替代原定 pokersolver) |
| 随机源 | 云函数内 `crypto` | 洗牌用 CSPRNG(`crypto.randomInt`) |

---

## 4. 数据模型

四个集合:

### 4.1 `rooms` — 房间/当前牌局状态(全房间订阅)

```js
{
  _id: "room_abc",
  roomCode: "A7X9",
  hostOpenid: "xxx",
  config: {
    sb: 10, bb: 20,                 // 盲注(开房者设,固定不涨)
    initialChips: 1000,             // 初始筹码(开房者设)
    gameType: "nlhe",
    loan: {                         // 默认 enabled=false;开启需手动填全部参数
      enabled: false,
      amount: 0,                    // 开启时必填:每次借款额
      cap: 0,                       // 累计借款上限,0=不限
      repayMultiplier: 1.0,         // 还款倍率(开启时必填,如 1.5)
      conditions: ["chipsZero"]     // 借款条件,首条=筹码归零
    },
    historyLimit: 10,               // 记录最近局数
    favoriteLimit: 10               // 收藏上限
  },
  status: "waiting|dealing|preflop|flop|turn|river|showdown|voting|closed",
  players: [
    { openid, nick, avatar, seat, chips, folded, allIn, bet, totalBet, active,
      acted,                         // 本轮是否已行动(P3 轮次结束判定 / 加注重开用),每街重置
      loan: 0, debt: 0, repaid: 0,   // 累计借入/应还/已还
      connected: true, lastSeen: 0 }
  ],
  dealerSeat: 0,
  communityCards: [],
  pot: 0,
  currentBet: 0,
  minRaise: 20,
  turnSeat: 1,
  bettingRound: "preflop",
  handNo: 1,
  version: 0,                       // CAS,防并发动作
  lastHandSnapshot: null,           // 上一手结算后、按钮移动前的快照(投票回退用),见 §9
  endVote: {
    active: false,
    initiator: "",
    yes: [], no: [],
    threshold: "all",               // 固定全员同意
    triggeredHandNo: 0
  },
  revealedHands: [],               // 当前手主动亮牌:[{openid, nick, holeCards, handNo}] 公开,见 §6.5(摊牌赢家由服务器自动写入)
  lastResult: null,                // 上一手结算结果面板(P4):{handNo, title, lines, potTotal, community, reveals, ts};大厅展示,startHand 时清空
  log: []                          // 环形截断至上限 50 条,防文档无限膨胀
}
```

> **此文档不含任何人的底牌(仅 `revealedHands` 里被主动亮出的牌)。** 所有玩家可实时监听。

### 4.2 `hands` — 每人私有底牌(仅本人可读)

```js
{ _id, roomId, ownerOpenid, holeCards: ["As","Kd"], handNo: 1 }
```
安全规则:`auth.openid == doc.ownerOpenid` 才可读;仅云函数管理员可写。

### 4.3 `handHistory` — 牌局记录(回看/收藏)

```js
{
  _id, roomId, handNo, ts,
  // holeCards 仅在 holeRevealed=true 时落库(赢家自动亮或主动开牌),否则存 [] — 防偷看
  players: [ { openid, nick, seat, holeCards, holeRevealed,
               finalChips, delta, loan, debt, repaid } ],
  communityCards: [...],
  winners: [ { openid, nick, hand, potShare } ],   // hand = 牌型文案(如「葫芦(A 带 K)」);弃牌独赢时为空串
  sidePots: [],                                    // P4 单池恒为 [],边池 P5 补
  actions: [ {ts, openid, text} ],                 // 当前手 log 流水切片(自最近一条「第 N 手开始」起)
  favorite: false,
  isFinal: false                     // 投票结束时的终局快照
}
```
- 写入:每手 `showdown` 或投票/房主结束时由云函数写一条。
- 读取:列表按 `ts desc` 取最近 `historyLimit` 条;收藏列表取 `favorite==true`,超 `favoriteLimit` 时新收藏挤掉最早的收藏。

### 4.4 `users` — 玩家昵称(可选)

```js
{ _id, openid, nick, avatar }
```

### 4.5 本地房间历史(仅客户端,非云集合)

首页「历史对局」按钮的数据源,**不落库**(朋友局单机重进场景价值低,零云成本):

```
wx storage key = 'poker_room_history'
[{ roomId, roomCode, ts }]   // 最近在前,LRU 上限 20 条
```

- 写入:`createRoom` / `joinRoom` / 历史页成功加入时记录(按 roomId 去重置顶)。
- 清理:点条目加入时若报 NOT_FOUND/CLOSED,自动移除该条并提示;长按可手动删,支持清空。
- 已知限制:不同设备不同步(换手机无历史);牌局进行中(status≠waiting)的房间暂不可从历史直接回桌,等 P7 重连方案(syncState)接入后打通。

---

## 5. 安全规则

| 集合 | 读 | 写 |
|------|----|----|
| `rooms` | 房间内成员(`auth.openid != null`) | 仅云函数(管理员 SDK) |
| `hands` | **仅 ownerOpenid 本人**(`auth.openid == doc.ownerOpenid`) | 仅云函数 |
| `handHistory` | 房间内成员(`auth.openid != null`) | 仅云函数 |
| `users` | 仅本人(`auth.openid == doc._openid`) | 仅云函数 |

- 云函数用 `@cloudbase/admin-node`(服务端 SDK + 环境密钥)写库,绕过安全规则。
- 客户端用户态 SDK 受规则约束。
- 关键:`hands` 永远只本人可读 → 防偷看底牌。

---

## 6. 核心流程

### 6.1 建房 / 加入
1. 开房者填盲注 / 初始筹码 / 借款配置(借款默认关,开则必填 amount+倍率)→ `createRoom` → 生成 roomCode、初始化 rooms 文档。
2. **初始 `lastHandSnapshot`** = 各玩家初始筹码状态(作为第 1 手的"上一手结束"基准)。
3. 分享房间号 / 小程序分享卡片。
4. 朋友输入房间号 → `joinRoom` → 入座(初始 chips = config.initialChips)。
5. 创建/加入成功即记入**本地房间历史**(§4.5),首页「历史对局」可一键重新加入。
5. 所有人 watch `rooms`,人齐房主点"开始"。

### 6.2 开始一手 `startHand`(P2 已实现)
1. 房主调 `startHand`:校验(仅房主 / status=waiting / ≥2 人)。
2. **庄家按钮**:第 1 手取最低 seat 并置 `dealerMoved=true`;此后顺时针移到下一座位(环形)。单挑时按钮即小盲位。
3. 云函数:CSPRNG Fisher–Yates 洗牌 → 发底牌 → 为每人写一条 `hands`(写库前先清本房间旧手记录,防跨手残留)。
4. 收盲注:≥3 人 SB=按钮下一位、BB=再下一位;**单挑按钮=SB、对家=BB**;筹码不足自动 all-in 截断。
5. 行动定位:preflop 从 BB 后一位开始(UTG);**单挑从按钮(SB)开始**。状态 preflop,version 走 CAS 更新(where 条件含 version,防并发)。
6. 客户端:`watch` 本人 `hands` 收底牌为主,房间状态变化时主动拉取兜底(admin 写库推送不可靠);watch `rooms` 看盲注/轮次。
7. 每手开始重置 `communityCards` / `revealedHands` / `endVote`;盲注合计入 `pot`,`currentBet=bb`,`minRaise=bb`。

> 阶段衔接说明:P2 时无结束一手的逻辑(action 未上线),需在云开发控制台手动把 preflop 房间的 `rooms.status` 改回 `waiting` 复用。P3/P4 上线后不再需要:全弃牌 → 剩者独赢自动回 `waiting`;打到摊牌则在 action 内原子结算后同样自动回 `waiting`,并留下「上局结果」面板。

### 6.3 玩家行动 `action`(P3 已实现)
客户端:`callFunction('action', { roomId, version, action, amount? })`,`amount` = **加注到的总额**(非增量);`allin` 忽略 amount 直接全下。动作:`fold / check / call / raise / allin`。
云函数:
1. CAS 校验 version(客户端传 watch 到的 rooms.version)。
2. 校验 status ∈ preflop/flop/turn/river、调用者 openid == turnSeat 玩家且未 fold 未 allIn。
3. 校验动作合法:
   - check:仅当 `currentBet == 本人bet`(有人下注时拒绝);
   - call:`delta = min(currentBet-bet, chips)`,短码自动截断为 all-in;
   - raise / allin:`raiseTo ≥ currentBet+minRaise`,**不足额仅允许纯 all-in**;chips 归零即置 allIn。
4. 更新 bet/chips/pot/currentBet/minRaise,**完整加注才重开行动权**:其余未 fold 未 allIn 者 `acted=false` 并更新 `minRaise=加注增量`;短码 all-in 不重开、不改 minRaise。推进 turnSeat = 下一个可行动座位(环形,跳过 folded/allIn)。
5. 判定本轮结束(所有未 fold 未 allIn 者均已 acted 且 bet == currentBet):
   - 结束 → 按下一街 CSPRNG 发公共牌(**排除所有 hands 底牌与已发公共牌**,从剩余牌抽):flop 3 张 / turn 1 张 / river 1 张;重置全员 bet→0、acted→false、currentBet→0、minRaise→bb;postflop 首个行动者 = **庄家后第一个可行动者(≥3 人即小盲;单挑即大盲)**。
   - 剩余可行动玩家 <2(全 all-in/弃光)→ 一次补齐剩余公共牌直接**原子结算**(§6.4);river 行动完同样原子结算。
6. 只剩 1 人未 fold(前面的人都弃了、后面没人)→ **直接获胜**:赢家拿全部底池,其底牌**默认 muck**,可随时调 `revealCards` 主动开;桌面公共牌**补发至完整 5 张**进结果面板展示(仅展示用,不影响已结算的输赢)。当前手立即收尾:`status → waiting`、`pot→0`、全员 `bet→0`、`handNo++`,房主可直接开下一手。

> P3/P4 实现注记:UI 上座位标注小盲/大盲身份(单挑庄家=D+小盲);行动条提示"面前有下注只能跟/加/弃"。摊牌在 action 内原子完成评牌分池并回 `waiting`(不再停留在 showdown 态),大厅用 `lastResult` 面板展示上一手结果(公共牌 + 赢家牌型文案 + 已亮底牌)。

### 6.4 摊牌与结算 `showdown`(P4 已实现,在 action 云函数内原子完成)
1. **内置评牌器**对未 fold 玩家 7 张牌 C(7,5)=21 组合穷举选最强 5 张打分(类别 + 决胜张数组逐一比较,A2345 轮子按 5 高处理)。
2. 排赢家;P4 单池平分,**余数按「庄家左侧起环序」逐一补给**;allin 不等栈边池属 P5(§11)。
3. 分池只把所赢筹码加进 chips,**不冲抵欠款**(定版变更,原「赢池自动还款」取消:随赢随清会让借款失去意义;欠款只通过手动 `repay` 和游戏结束强制结清,见 §10.3)。
4. 各玩家 `debt` / `repaid` 在分池时保持不变。
5. **开牌语义**(多人到摊牌时):**所有未弃牌玩家**的底牌均写回 `revealedHands`,各附评出的牌型文案(用户定版:手终摊牌全亮);已弃牌的人默认不亮(muck),可各自随时调 `revealCards` 主动开。详见 §6.5。
6. 写一条 `handHistory`(两种手终都写:摊牌与弃牌独赢;未亮者不落底牌)。
7. **写入 `lastHandSnapshot`** = 当前各玩家 chips/dealerSeat/communityCards 等结算后状态(**此时庄家按钮尚未移动到下一手**),作为下次投票的回退点。**弃牌独赢收尾同样写快照**(该手已是"判完输赢、按钮未移"的公平时点,语义一致)。
8. handNo++,写 `lastResult` 结果面板,状态 → waiting,等房主开下一手(startHand 时清 `lastResult`)。

> 实现细节:P3 的 action CAS 更新原先漏了 `version` 自增,P4 一并补上(`version: _.inc(1)`),并发/重放防护才真正生效(§13)。结算路径含多次串行库操作(查 rooms → 发补牌/评牌共用一次 hands 查询 → CAS 更新 → 写流水),默认 3 秒超时不够,action 已配 `config.json` `timeout=20`;主入口统一 try/catch 归一异常返回,前端能看到真实错误而不是笼统的 -504002。

### 6.5 开牌 / 亮牌规则(`revealCards`)
对应你定的两条规则:

| 场景 | 默认 | 可选开牌 |
|------|------|---------|
| 当前手只剩一人未弃牌 → 直接获胜 | 赢家底牌**不亮**(muck);桌面公共牌**补发至完整 5 张**进结果面板展示(仅展示,不影响输赢) | 赢家可主动 `revealCards` 开 |
| 已弃牌的人(任何时候,含当前手进行中或已结束) | **不亮** | 可主动 `revealCards` 开(给朋友看/诈唬秀牌) |
| 多人到摊牌结算 | **所有未弃牌玩家底牌全亮**(写回 `revealedHands`,附牌型文案;用户定版) | 弃牌的人均可主动 `revealCards` 开 |

- 实现:`revealCards({roomId})` 云函数,校验调用者当前手有 `hands` 记录 → 把其 `holeCards` 追加进 `rooms.revealedHands`(公开),所有人 watch 实时看到。
- `revealedHands` 在每手开始时清空(`startHand` 时重置为 `[]`)。
- 服务器评牌始终用真实底牌(云函数内部),只是**是否对外公布**受上述规则控制;不公布不影响判定,只影响别人看不看得到。
- 即:判定胜负在云函数内始终完成,亮牌只是"要不要把牌晒出来给其他人看"。

---

## 7. 云函数清单

| 函数 | 触发 | 职责 |
|-----|------|------|
| `createRoom` | 客户端 | 建房,写 config(盲注/筹码/借款),初始快照 |
| `joinRoom` | 客户端 | 入座 |
| `leaveRoom` | 客户端 | 离座;房主踢人 |
| `startHand` | 客户端(房主) | 洗牌发牌、收盲注 |
| `action` | 客户端 | 处理行动 + 推进 |
| `borrow` | 客户端 | 借款(校验条件) |
| `repay` | 客户端(可选) | 手动还款 |
| `proposeEnd` | 客户端 | 发起结束投票 |
| `voteEnd` | 客户端 | 投同意/反对,全员同意则回退 + `endGame` |
| `endGame` | voteEnd / 房主 | 强制还款、算余额(可负)、写终局 history、status=closed |
| `favoriteHand` | 客户端 | 收藏/取消,超 10 挤掉最早 |
| `revealCards` | 客户端 | 主动亮本人底牌(赢家/弃牌者/摊牌未赢者均可),写入 revealedHands |
| `syncState` | 客户端 | 重连拉全量 |
| `showdown` | action 内部 | 评牌、分池、借款还款、结算、写快照 |

> 所有写库统一走管理员 SDK。

---

## 8. 重连与状态记录

- **每手状态全程在库**:`rooms`(公开)+ `hands`(私有)即牌局全部状态,无需额外快照。
- **watch 自动续接**:客户端断线后 `watch` 自动重连推送,恢复后 UI 立即同步到最新。
- **重连拉全量**:若 watch 错过更新,调 `syncState(roomId)` 取 `rooms` + 本人 `hands`。
- **离线玩家不阻塞?** 出牌无时限,离线玩家轮到时桌面暂停。处理:
  - 其他人可发起"投票结束整局"(§9),或
  - 房主可对该离线玩家 `leaveRoom`/标记 fold 推进。
  - `players[].connected` / `lastSeen` 判断在线(客户端心跳更新 lastSeen)。
- **牌局回看**:`handHistory` 记每手完整动作流水 + 摊牌结果,列表取最近 10 条 + 收藏(上限 10),点击重放。

---

## 9. 投票结束整局(手级回退)

### 9.1 快照机制(手级,非轮级)
- `rooms.lastHandSnapshot`:**每手结算后**(`showdown` 第 7 步)写入一条快照,内容:
  `{ handNo, players:[{seat,chips,loan,debt,repaid,folded}], dealerSeat, communityCards }`。
- 关键时点:此时**庄家按钮尚未移动到下一手**(`startHand` 才移动按钮),正好对应"上一手开完牌、判完输赢、庄家移动前"。
- 房间刚建好时(第 1 手前),`lastHandSnapshot` = 各玩家初始筹码状态。

### 9.2 投票流程
1. 某下注轮结束 → 任一玩家调 `proposeEnd` → `endVote.active=true`,记录 initiator 与 `triggeredHandNo`(当前手号)。
2. 其他人调 `voteEnd({agree})` → 累加 yes/no。
3. **全员同意**(threshold=all,活跃玩家全投 yes)→ 进入回退 + `endGame`:
   a. **当前手无效**:若投票发生在某手进行中或某手刚结束,该手(及其中所有下注)整体作废。
   b. **回退到上一手结算后、按钮移动前**:把 `rooms` 的 `players`(chips/loan/debt/repaid/folded)、`dealerSeat`、`communityCards` 还原到 `lastHandSnapshot`。当前手的下注随之退还(快照里是结算后的筹码,不含当前手)。
4. 任一玩家投反对,或新一手开始 → 重置 `endVote`。

### 9.3 回退后结束游戏
- 回退后调 `endGame`(§10.4):
  1. (无需再 showdown——回退点已是上一手判完输赢后的状态,当前手已作废。)
  2. 对每个玩家强制还款:`chips -= debt; debt=0`(可为负)。
  3. 计算各玩家最终余额 = chips(正/零/负)。
  4. 写一条 `isFinal=true` 的 `handHistory` 终局快照(含每人 loan/debt/finalChips)。
  5. `rooms.status = closed`。

> 设计意图:投票通过 = 当前手白打,整局停在"上一手判完输赢、庄家还没移位"的公平时点清算,然后结束游戏。无需对作废的当前手做 showdown,因为回退点本身已经是上一手的结算结果。

---

## 10. 借款与还款

### 10.1 配置
- `config.loan.enabled` **默认 false**;开房者开启借款时必须手动填 `amount`(每次借款额)与 `repayMultiplier`(还款倍率),否则不允许开启。
- `conditions: ["chipsZero"]` 当前唯一条件:筹码归零方可借。
- 倍率 1.0 = 无息;1.5 = 借 100 还 150。

### 10.2 借款 `borrow`
- 校验:`enabled==true` && `chips==0` && (cap=0 或 `loan+amount<=cap`)。
- 执行:`chips += amount`;`loan += amount`;`debt = loan × repayMultiplier`。
- 借款为非动作动作,在轮到自己时可做,不影响下注推进。

### 10.3 还款(P4 定版修订:取消「赢池自动冲抵」)
- ~~自动:分池时赢家净盈利优先冲抵 `debt~~`(已删除:随赢随清让借款失去意义)。
- **手动**:`repay({amount})` 用现有 chips 主动还:`chips -= amount; debt -= amount; repaid += amount`。
- **游戏结束强制还款**(endGame,关键):
  对每个玩家:`chips -= debt; debt = 0`(一次性还清)。
  **若 chips < debt,余额照扣,最终 chips 可为负**(表示净欠账),记入 `handHistory` 的 `debt`/负 `finalChips`。
  即"即便负数也照样扣除"。
- 即:P6 上线后欠款的唯一结清途径 = 玩家手动还 或 endGame 强制清算。

### 10.4 endGame 完整顺序
1. 回退点已是上一手结算后状态(见 §9.3),无需再 showdown。
2. 对每个玩家强制还款:`chips -= debt; debt=0`(可为负)。
3. 计算各玩家最终余额 = chips(正/零/负)。
4. 写一条 `isFinal=true` 的 `handHistory` 终局快照(含每人 loan/debt/finalChips)。
5. `rooms.status = closed`。

---

## 11. 边池算法

```
按 totalBet 升序遍历未 fold 玩家:
  levelBet = 当前层下注
  pot = levelBet × (本层及之后所有未fold玩家数)
  本层及之后玩家评牌,赢家拿 pot
  已分配玩家移出,下一层继续
```
all-in 玩家只赢到等额下注的池子,超出归外池。朋友局可先单池近似,P5 再补完整。

---

## 12. 前端结构

### 12.1 卡图资源(P2 实际采用)
**纯 CSS 绘制**,零图片/素材依赖:`components/card` 组件解析 `"As"` 格式牌名,渲染 rank + 花色符号(♠♥♦♣ 红黑分色)、深红斜纹牌背、虚线空位三种形态,sm/md/lg 三档尺寸。
后续拿到美术资源可整体替换为雪碧图方案(1 张 52+背面,CSS background-position 切牌),组件接口不变。

### 12.2 模块划分
```
components/
  Card/ PlayerSeat/ CommunityCards/ ActionBar/ Pot/
  LoanPanel/        // 借款/还款/欠款显示
  VoteBar/          // 投票结束弹窗
  HistoryList/     // 回看列表 + 收藏
  ReconnectMask/    // 断线重连提示
hooks/
  useRoom(roomId)   // watch rooms
  useMyHand(roomId) // watch 本人 hands
pages/
  history/          // 历史对局:本地记录的曾加入房间,点击重新加入(§4.5)
services/
  cloud.js         // callFunction + wx.cloud 初始化
  actions.js       // fold/call/raise/borrow/repay/proposeEnd/voteEnd/revealCards
  history.js       // 本地房间历史(wx storage,LRU 上限 20)
```

---

## 13. 反作弊要点

| 风险 | 防御 |
|------|------|
| 偷看底牌 | `hands` 仅本人可读 |
| 伪造发牌 | 发牌只在云函数 |
| 越权代打 | action 校验 openid == turnSeat |
| 并发/重放 | rooms.version CAS |
| 改筹码/借款 | 仅云函数管理员 SDK 写 |
| 投票作弊 | voteEnd 校验 openid 唯一票、活跃玩家全员 yes 才通过 |

---

## 14. 分阶段实施计划

| 阶段 | 内容 | 验收 |
|------|------|------|
| P0 环境 | 开通云开发、建集合、配安全规则、openid 登录跑通 | 能进、能看到房间 |
| P1 房间 | createRoom(盲注/筹码/借款配置,借款默认关)/joinRoom/leaveRoom + watch | 朋友进同房,配置生效 |
| P2 发牌 | startHand 洗牌发底牌 + hands 私有下发 + 卡图 | 每人看到自己 2 张牌 |
| P3 下注 | action + preflop 盲注轮转 + UI 按钮 | 能完整打一手到摊牌 |
| P4 结算 | flop/turn/river + 牌型判定 + 单池 + handHistory 落库 + lastHandSnapshot | 判定赢家、记一局、留回退点 |
| P5 边池 | allin 不等栈边池分配 | 多栈 allin 正确分池 |
| P6 借款 | borrow/repay(手动)+ 欠款显示;借款默认关、开启必填参数;**分池不自动冲抵欠款**(§10.3 定版) | 归零可借、手动还、终局强制扣(可为负) |
| P7 重连 | syncState + watch 续接 + 离线状态 | 断线重连不丢进度 |
| P8 投票 | proposeEnd/voteEnd + lastHandSnapshot 回退 + endGame 强制还款 | 全员同意→回退上一手→结束并清算(余额可负) |
| P9 回看 | handHistory 列表 + 收藏(上限 10)+ 重放动画 | 翻看最近 10 局与收藏局 |

P0–P3 跑通最小闭环,P4 起补结算与周边。

### 14.1 测试约束:身份即 openid,单账号无法模拟多人

- 玩家身份 = 微信 openid,由微信平台下发,**同一微信号在任意端(开发者工具模拟器 / 手机预览 / 体验版)都是同一个 openid**。
- 因此**无法用一个微信号同时扮演两个玩家**:第二个"玩家"调 `joinRoom` 会命中幂等分支(该 openid 已入座),直接返回成功但不新增座位——表现为"房间里只有房主一个人",数据库 `players` 数组也只有 1 条。
- **多人测试必须用多个真实微信号**:小程序后台「成员管理 → 体验成员」添加朋友/家人的微信号,对方扫码预览或用体验版进入。开发期可注册一个备用小号专门当测试对手。
- 相关实现细节:
  - `joinRoom` 对已入座 openid 幂等返回 `{alreadyIn:true}` 并照常跳房间页(不报错、不重复入座)——这是防重复入座的正确行为,不是 bug;
  - 安全规则里 `"write": "false"` 必须是裸布尔,带引号的 `"false"` 是字符串会导致规则无效;自定义安全规则仅接受 `read`/`write` 两键,不能加 `comment`;
  - 自定义安全规则的客户端身份标识是 **`auth.openid`**(微信小程序登录态),不是 `auth.uid`(那是 CloudBase 独立部署/自定义登录体系的字段)——用错字段会表现为所有读请求 `-502003 database permission denied`;
  - **新增云函数必须在开发者工具里右键「上传并部署」**,漏传表现为调用报 `-501000 FUNCTION_NOT_FOUND`;开发者工具环境与 `config.js` 的 `cloudEnvId` 不指向同一环境时同样报此错;
  - **纯对象字段整体更新必须用 `_.set()`**:`update({data:{objField: {...}}})` 会被展开成子路径写入,若该字段当前值是 `null`(如 startHand 重置过的 `lastResult`)会报 `Cannot create field 'xxx' in element {lastResult: null}`(-502001)导致整个 update 失败;数组字段不受影响。`lastResult` / `lastHandSnapshot` 均已用 `_.set()` 包裹;
  - **isHost 等前端身份判定依赖 login 成功**:login 云函数未部署/失败时 openid 为空,建房入座仍正常(openid 由云函数服务端取),但房主看不到开局按钮 —— 报「登录失败」先查 FUNCTION_NOT_FOUND;
  - 云函数端写库对客户端 watch 的实时推送不可靠(CloudBase 已知限制),若后续阶段实测推送缺失,兜底方案是客户端轮询拉文档(P7 重连阶段一并处理)。

---

## 15. 成本估算

- 10 人朋友局,一手约 200 次库写、2000 次实时读。
- 一天 20 手 ≈ 4000 写 / 4万读;月 ~12万写 / 120万读。
- 云函数调用同量级,百万次 ~¥1。
- handHistory 写入稀疏(每手 1 条)。
- **预估月费:¥0–¥5,基本卡在免费额度内。**

---

## 16. 已确认参数

1. 盲注:**固定不涨**。
2. 借款:默认关闭;开启需手动设 amount + 倍率;游戏结束**强制还款**,余额可为负照扣。
3. 投票结束阈值:**全员同意**。
4. 投票通过:**当前手无效,回退到"上一手开完牌、判完输赢、庄家按钮移动前"的状态**(经 `lastHandSnapshot` 还原),再结束游戏清算;作废的当前手无需 showdown。
5. 收藏:**上限 10 局**,超限挤掉最早的收藏。
6. 开牌-唯一剩者(P4 修订):当前手只剩一人未弃牌 → 直接获胜;赢家底牌**默认不亮**(可选开);桌面公共牌**补发至完整 5 张**进结果面板展示(仅展示用)。
7. 开牌-摊牌/弃牌者(P4 修订):多人到摊牌,**所有未弃牌玩家底牌全亮**(附牌型文案);已弃牌者任何时候默认不亮,可选主动开。

参数已全部敲定。可按 P0→P3 出最小可跑代码骨架(云函数 + 小程序 UI)。需要我开始写代码就说一声。
